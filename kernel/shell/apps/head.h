#ifndef NEXUSOS_HEAD_H
#define NEXUSOS_HEAD_H
/* head <file> [n] — печатает первые n символов файла (по умолчанию 20).
 * Файлы здесь однострочные, поэтому "head" тут про символы, не строки. */
void head_run(char *args);
#endif
