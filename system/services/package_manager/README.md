# Nexus Package Manager

`.nx` is the Nexus Package container extension. The first implementation only
defines metadata and discovery boundaries; it does not execute arbitrary package
binaries and does not download packages from a network.
