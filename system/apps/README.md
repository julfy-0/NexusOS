# NexusOS System Applications

Built-in applications remain part of the current kernel-side GUI. This directory
is the runtime location reserved for future `.nx` packages; it does not pretend
to execute package binaries yet.
